<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="5dp"
    android:layout_marginVertical="5dp"
    android:background="@drawable/bg_glass_card"
    android:orientation="vertical"
    android:padding="10dp">

    <!-- Üst Satır: Kategori ve Seçenekler -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_post_category"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/bg_category_badge"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingHorizontal="7dp"
            android:paddingVertical="3dp"
            android:textColor="@color/gold_start"
            android:textSize="10sp"
            android:textStyle="bold"
            tools:text="Tefsir" />

        <ImageView
            android:id="@+id/btn_post_options"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="Seçenekler"
            android:focusable="true"
            android:padding="4dp"
            android:src="@drawable/ic_more_vert"
            app:tint="@color/text_secondary" />
    </LinearLayout>

    <!-- PDF Kapak Önizlemesi (Kompakt ve Dikdörtgen) -->
    <FrameLayout
        android:id="@+id/container_cover_preview"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_tab_container"
        android:clickable="true"
        android:focusable="true">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_pdf_cover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="PDF Önizlemesi"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_academic_parchment_cover"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Small" />

        <TextView
            android:id="@+id/tv_pdf_size"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="6dp"
            android:background="@drawable/bg_glass_badge"
            android:paddingHorizontal="5dp"
            android:paddingVertical="2dp"
            android:textColor="@color/text_primary"
            android:textSize="9sp"
            android:textStyle="bold"
            tools:text="2.4 MB" />
    </FrameLayout>

    <!-- Risale Başlığı -->
    <TextView
        android:id="@+id/tv_post_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:lineSpacingExtra="1dp"
        android:maxLines="2"
        android:textColor="@color/text_primary"
        android:textSize="13sp"
        android:textStyle="bold"
        tools:text="Klasik Dönem Tefsir Yöntemleri" />

    <!-- Risale Açıklaması -->
    <TextView
        android:id="@+id/tv_post_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:lineSpacingExtra="1dp"
        android:maxLines="2"
        android:textColor="@color/text_secondary"
        android:textSize="11sp"
        tools:text="Bu çalışma klasik dönem müfessirlerinin dirayet ve rivayet yöntemlerini karşılaştırmaktadır." />

    <!-- Yazar Bilgisi (Avatar + İsim) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_author_avatar"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_academic_logo"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_author_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_primary"
                android:textSize="11sp"
                android:textStyle="bold"
                tools:text="Ahmet Farabi" />

            <!-- Gizli tutulabilen veya küçük unvan -->
            <TextView
                android:id="@+id/tv_author_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_secondary"
                android:textSize="9sp"
                android:visibility="gone"
                tools:text="Araştırmacı" />
        </LinearLayout>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginVertical="6dp"
        android:background="#22FFFFFF" />

    <!-- Alt Etkileşim Satırı: Beğeni, Yorum, Oku (Tablette Taşmayı Önleyen Responsive Düzen) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical">

        <!-- Sol Grup: Beğeni ve Yorum -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/btn_read_pdf"
            android:layout_marginEnd="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Beğeni -->
            <LinearLayout
                android:id="@+id/btn_post_like"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:padding="3dp">

                <ImageView
                    android:id="@+id/iv_like_icon"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:contentDescription="Beğen"
                    android:src="@drawable/ic_heart_outline"
                    app:tint="@color/text_secondary" />

                <TextView
                    android:id="@+id/tv_like_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="3dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="10sp"
                    tools:text="14" />
            </LinearLayout>

            <!-- Yorum -->
            <LinearLayout
                android:id="@+id/btn_post_comment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:padding="3dp">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:contentDescription="Yorumlar"
                    android:src="@drawable/ic_comment_outline"
                    app:tint="@color/text_secondary" />

                <TextView
                    android:id="@+id/tv_comment_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="3dp"
                    android:textColor="@color/text_secondary"
                    android:textSize="10sp"
                    tools:text="3" />
            </LinearLayout>
        </LinearLayout>

        <ImageView
            android:id="@+id/btn_post_share"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            android:contentDescription="Paylaş" />

        <!-- Oku Butonu (Göz İkonu): Kartın İçinde Sabit, Asla Taşmaz -->
        <FrameLayout
            android:id="@+id/btn_read_pdf"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_btn_gold_vibrant_pill"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_gravity="center"
                android:contentDescription="Mütalaa Et"
                android:src="@drawable/ic_eye"
                app:tint="@color/splash_bg_start" />
        </FrameLayout>

        <!-- Tarih gizli tutulabilir veya küçük metin -->
        <TextView
            android:id="@+id/tv_post_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            tools:text="2s" />
    </RelativeLayout>

</LinearLayout>
